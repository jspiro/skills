# Personal

jspiro's own skills — the reason this fork exists. Everything else in
`skills/` is inherited from [mattpocock/skills](https://github.com/mattpocock/skills).

- [`internalize`](./internalize/SKILL.md) — Turn corrective feedback into a
  durable CLAUDE.md rule: reflect the lesson back in its own words, iterate
  until confirmed, persist it in the right CLAUDE.md tier, red-team the
  written rule, then fix the original work.
- [`jspiro-git`](./jspiro-git/SKILL.md) — jspiro's git preferences: branch
  discipline, individual staging, SSH-signing workflow with a locked-vault
  fallback, and hard identity guardrails. Useful as a template for writing
  your own git-preferences skill.
